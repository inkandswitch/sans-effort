[package]
name = "routines"
version = "0.1.0"
description = "The demo routines — a greeter, its fan-out variant, and a ticker — written against capability traits. They know nothing of effects, handles, drivers, or hosts"
publish = false

authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
sans_effort = { workspace = true }

[dev-dependencies]
bolero = { workspace = true }
sans_effort = { workspace = true, features = ["std"] }
trybuild = { workspace = true }

[lints]
workspace = true
